import styled from 'styled-components'
import { secondaryColor } from 'styles/variables.styles'

export const FileName = styled.p`
  margin: 0px;
`

export const FileSize = styled.span`
  color: ${secondaryColor};
`
